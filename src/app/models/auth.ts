export interface LoginData {
    email: string;
    password: string;
}

export interface RegisterData {
    userName: string;
    userSurname: string;
    email: string;
    password: string;
    birthDate: string;
}
